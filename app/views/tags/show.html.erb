<div class="h-feed">
	<header>
		<h1 class="p-name"><%= t('tags.show.title', tag: @tag) %></h1>
	</header>

	<%- if @results.any? -%>
		<ol reversed>
			<%- @results.each do |result| -%>
				<li>
					<div class="h-entry">
						<%= link_to result.title, article_path(result), class: 'u-url p-name' %>
					</div>
				</li>
			<%- end -%>
		</ol>
	<%- else -%>
		<p><%= t('tags.show.none', tag: @tag) %></p>
	<%- end -%>
</div>
